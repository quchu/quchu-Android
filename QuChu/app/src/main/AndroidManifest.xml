<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.quchu.quchu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name=".base.AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
            <!--android:name="co.quchu.quchu.view.activity.PlanetActivity"-->
        <activity
            android:name="co.quchu.quchu.view.activity.PlanetActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
     <!--   <activity
            android:name="co.quchu.quchu.view.activity.PlanetActivity"
            android:label="@string/titlebar_word_planet" />-->
        <activity
            android:name="co.quchu.quchu.view.activity.GeneActivity"
            android:label="@string/titlebar_word_gene" />
        <activity
            android:name="co.quchu.quchu.view.activity.GeneIntroduceActivity"
            android:launchMode="singleInstance"
            android:label="@string/titlebar_word_gene_introduce" />
        <activity
            android:name="co.quchu.quchu.view.activity.FriendsCircleIntroduceActivity"
            android:launchMode="singleInstance"
            android:label="@string/titlebar_word_friends_introduce" />
        <activity
            android:name="co.quchu.quchu.view.activity.AtmosphereActivity"
            android:launchMode="singleInstance"
            android:label="@string/titlebar_word_atmosphere" />
        <activity
            android:name="co.quchu.quchu.view.activity.DiscoverActivity"
            android:launchMode="singleInstance"
            android:label="@string/titlebar_word_discover" />
        <activity
            android:name="co.quchu.quchu.view.activity.QuFriendsActivity"
            android:launchMode="singleInstance"
            android:label="@string/titlebar_word_qu_friends" />
    </application>

</manifest>
